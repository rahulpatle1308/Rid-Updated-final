<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Birthday Celebration</title>
    <link rel="icon" type="image/png" href="/assets/signup.png">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Dancing+Script:wght@700&family=Poppins:wght@300;400;600;700&family=Great+Vibes&family=Sacramento&family=Playfair+Display:wght@700&family=Cinzel:wght@700&family=Parisienne&family=Amatic+SC:wght@700&family=WindSong:wght@500&family=Rouge+Script&family=Mr+De+Haviland&family=Qwitcher+Grypen:wght@700&family=Caveat:wght@700&family=Pacifico&family=Marck+Script&family=Gloria+Hallelujah&family=Indie+Flower&display=swap" rel="stylesheet">
    <style>
        /* RESET & BASE STYLES */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        html {
            scroll-behavior: smooth;
            -webkit-text-size-adjust: 100%;
            -webkit-tap-highlight-color: transparent;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: #000;
            color: #fff;
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
            background:
                radial-gradient(circle at 20% 30%, rgba(120, 0, 80, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 80% 70%, rgba(0, 100, 120, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 40% 80%, rgba(120, 80, 0, 0.3) 0%, transparent 50%),
                linear-gradient(135deg, #0a0a2a 0%, #1a0033 30%, #33001a 70%, #000d33 100%);
            -webkit-font-smoothing: antialiased;
            -moz-osx-font-smoothing: grayscale;
        }

        /* ANIMATED NAVBAR - SAME AS HOME PAGE */
        .navbar {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 1000;
            background: rgba(10, 10, 42, 0.95);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border-bottom: 2px solid rgba(255, 102, 204, 0.3);
            padding: 15px 0;
            transform: translateY(-100%);
            animation: slideDown 1s forwards;
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5);
        }

        @keyframes slideDown {
            to {
                transform: translateY(0);
            }
        }

        .nav-container {
            max-width: 1400px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            height: 70px;
        }

        .nav-logo {
            font-family: 'Great Vibes', cursive;
            font-size: 2.2rem;
            background: linear-gradient(to right, #ff66cc, #ffcc00);
            -webkit-background-clip: text;
            background-clip: text;
            color: transparent;
            text-decoration: none;
            position: relative;
            padding-right: 15px;
            white-space: nowrap;
        }

        .nav-logo::after {
            content: '';
            position: absolute;
            right: 0;
            top: 50%;
            transform: translateY(-50%);
            width: 2px;
            height: 30px;
            background: linear-gradient(to bottom, #ff66cc, #ffcc00);
            animation: pulseGlow 2s infinite;
        }

        /* HAMBURGER MENU */
.hamburger {
    display: none; /* Desktop par hide */
    cursor: pointer;
    background: rgba(255, 102, 204, 0.1);
    border: none;
    color: white;
    font-size: 1.8rem;
    width: 50px;
    height: 50px;
    border-radius: 50%;
    align-items: center;
    justify-content: center;
    transition: all 0.3s ease;
    z-index: 1001;
}

.hamburger:hover {
    background: rgba(255, 102, 204, 0.2);
    transform: scale(1.05);
}

/* Mobile par show */
@media (max-width: 768px) {
    .hamburger {
        display: flex;
    }
}


        .nav-menu {
            display: flex;
            gap: 25px;
            list-style: none;
            margin: 0;
            padding: 0;
        }

        .nav-item {
            position: relative;
        }

        .nav-link {
            color: #fff;
            text-decoration: none;
            font-size: 1.1rem;
            font-weight: 600;
            padding: 10px 18px;
            border-radius: 50px;
            transition: all 0.4s ease;
            display: flex;
            align-items: center;
            gap: 8px;
            position: relative;
            overflow: hidden;
            white-space: nowrap;
        }

        .nav-link::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.2), transparent);
            transition: left 0.7s;
        }

        .nav-link:hover::before {
            left: 100%;
        }

        .nav-link:hover {
            background: linear-gradient(135deg, rgba(255, 102, 204, 0.2), rgba(255, 204, 0, 0.2));
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(255, 102, 204, 0.3);
        }

        .nav-link.active {
            background: linear-gradient(135deg, #ff0066, #ff6600);
            box-shadow: 0 0 30px rgba(255, 102, 204, 0.5);
        }

        .nav-icon {
            font-size: 1.3rem;
            animation: rotateIcon 3s infinite linear;
        }

        @keyframes pulseGlow {

            0%,
            100% {
                opacity: 0.5;
            }

            50% {
                opacity: 1;
            }
        }

        @keyframes rotateIcon {
            from {
                transform: rotate(0deg);
            }

            to {
                transform: rotate(360deg);
            }
        }

        /* MEMORIES SECTION STYLES */
        .memories-section {
            padding-top: 100px;
            min-height: 100vh;
            position: relative;
            overflow: hidden;
        }

        /* Stars Background */
        .stars {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            z-index: -1;
            pointer-events: none;
        }

        .star {
            position: absolute;
            background-color: #fff;
            border-radius: 50%;
            animation: twinkle 5s infinite;
        }

        @keyframes twinkle {

            0%,
            100% {
                opacity: 0.2;
            }

            50% {
                opacity: 1;
            }
        }

        /* Memories Title Container */
        .memories-title-container {
            text-align: center;
            padding: 40px 20px;
            position: relative;
            z-index: 10;
        }

        .memories-main-title {
            font-family: 'Dancing Script', cursive;
            font-size: 4rem;
            color: #ffcc00;
            margin-bottom: 20px;
            text-shadow: 0 0 30px rgba(255, 204, 0, 0.5);
            animation: titleSparkle 4s infinite alternate;
        }

        .memories-subtitle {
            font-family: 'Great Vibes', cursive;
            font-size: 2.5rem;
            color: #ff66cc;
            margin-bottom: 30px;
        }

        .heart-divider {
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 20px;
            margin: 30px 0;
        }

        .heart-divider-line {
            width: 150px;
            height: 2px;
            background: linear-gradient(90deg, transparent, #ff66cc, transparent);
        }

        .heart-divider-icon {
            font-size: 2rem;
            color: #ff3366;
            animation: heartbeat 1.5s infinite;
        }

        /* Album Container */
        .album-container {
            max-width: 800px;
            margin: 40px auto;
            padding: 0 20px;
            perspective: 1000px;
        }

        .photo-album {
            position: relative;
            width: 100%;
            height: 500px;
            transform-style: preserve-3d;
            transform-origin: left center;
            transition: transform 2s cubic-bezier(0.68, -0.55, 0.265, 1.55);
            cursor: pointer;
            animation: albumFloat 8s infinite ease-in-out;
        }

        .photo-album.open {
            transform: rotateY(-180deg);
        }

        .album-cover,
        .album-content {
            position: absolute;
            width: 100%;
            height: 100%;
            backface-visibility: hidden;
            border-radius: 20px;
            overflow: hidden;
            padding: 40px;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .album-cover {
            background: linear-gradient(135deg, #0066ff, #6600ff, #cc00ff);
            box-shadow: 0 30px 80px rgba(102, 0, 255, 0.5),
                inset 0 0 60px rgba(255, 255, 255, 0.1);
        }

        .album-content {
            background: linear-gradient(135deg, #1a0033, #33001a);
            transform: rotateY(180deg);
        }

        .album-lock {
            width: 100px;
            height: 100px;
            background: radial-gradient(circle, #00ccff, #0066ff);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            margin-bottom: 30px;
            cursor: pointer;
            box-shadow: 0 0 50px #00ccff;
            animation: lockGlow 3s infinite alternate;
            transition: all 0.3s ease;
        }

        .album-lock:hover {
            transform: scale(1.1);
            box-shadow: 0 0 70px #00ccff;
        }

        .album-lock-icon {
            font-size: 3rem;
            color: white;
        }

        .album-instruction {
            margin-top: 30px;
            color: white;
            font-size: 1.2rem;
            animation: pulseInstruction 2s infinite;
        }

        /* Memory Grid */
        .memory-grid {
            display: grid;
            grid-template-columns: repeat(3, 1fr);
            gap: 20px;
            width: 100%;
            max-width: 600px;
            margin: 0 auto;
        }

        .memory-item {
            background: linear-gradient(135deg, rgba(255, 102, 204, 0.2), rgba(102, 0, 255, 0.2));
            border-radius: 15px;
            height: 120px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 3rem;
            cursor: pointer;
            transition: all 0.3s ease;
            border: 2px solid transparent;
        }

        .memory-item:hover {
            transform: scale(1.1) rotate(5deg);
            background: linear-gradient(135deg, rgba(255, 102, 204, 0.4), rgba(102, 0, 255, 0.4));
            border-color: #ff66cc;
            box-shadow: 0 10px 30px rgba(255, 102, 204, 0.4);
        }

        /* Memory Modal */
        .memory-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(10px);
            z-index: 2000;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .memory-modal.active {
            display: flex;
        }

        .memory-modal-content {
            background: linear-gradient(135deg, #1a0033, #33001a);
            max-width: 500px;
            width: 95%;
            border-radius: 20px;
            padding: 40px;
            position: relative;
            box-shadow: 0 30px 80px rgba(102, 0, 255, 0.5);
            border: 2px solid rgba(255, 102, 204, 0.3);
            animation: popupSlideUp 0.5s ease;
        }

        .close-memory {
            position: absolute;
            top: 15px;
            right: 15px;
            width: 40px;
            height: 40px;
            border-radius: 50%;
            background: linear-gradient(135deg, #ff0066, #ff6600);
            border: none;
            color: white;
            font-size: 1.2rem;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: all 0.3s ease;
        }

        .close-memory:hover {
            transform: scale(1.1) rotate(90deg);
            background: linear-gradient(135deg, #ff6600, #ff0066);
        }

        .memory-modal-content h3 {
            font-family: 'Dancing Script', cursive;
            font-size: 2.5rem;
            color: #ffcc00;
            margin-bottom: 20px;
        }

        .memory-modal-content p {
            color: #ccccff;
            font-size: 1.1rem;
            line-height: 1.6;
        }

        /* Permission Modal */
        .memories-permission-modal {
            display: none;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: rgba(0, 0, 0, 0.9);
            backdrop-filter: blur(10px);
            z-index: 2000;
            justify-content: center;
            align-items: center;
            padding: 20px;
        }

        .memories-permission-modal.active {
            display: flex;
        }

        .memories-permission-content {
            background: linear-gradient(135deg, #1a0033, #33001a);
            max-width: 600px;
            width: 95%;
            border-radius: 20px;
            padding: 40px;
            text-align: center;
            box-shadow: 0 30px 80px rgba(102, 0, 255, 0.5);
            border: 2px solid rgba(255, 102, 204, 0.3);
            animation: popupSlideUp 0.5s ease;
        }

        .memories-permission-content h2 {
            font-family: 'Dancing Script', cursive;
            font-size: 3rem;
            color: #ffcc00;
            margin-bottom: 20px;
        }

        .memories-permission-content p {
            color: #ccccff;
            font-size: 1.2rem;
            margin-bottom: 20px;
            line-height: 1.6;
        }

        .memories-permission-buttons {
            display: flex;
            gap: 20px;
            justify-content: center;
            margin-top: 30px;
            flex-wrap: wrap;
        }

        .memories-permission-btn {
            padding: 15px 30px;
            border: none;
            border-radius: 50px;
            font-family: 'Poppins', sans-serif;
            font-size: 1.1rem;
            font-weight: 600;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            gap: 10px;
            color: white;
            min-width: 250px;
            justify-content: center;
        }

        .memories-yes-btn {
            background: linear-gradient(135deg, #00cc66, #00ff99);
        }

        .memories-no-btn {
            background: linear-gradient(135deg, #6666ff, #9999ff);
        }

        .memories-permission-btn:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
        }

        /* Animations */
        @keyframes albumFloat {

            0%,
            100% {
                transform: translateY(0) rotateY(0deg);
            }

            50% {
                transform: translateY(-20px) rotateY(5deg);
            }
        }

        @keyframes lockGlow {
            0% {
                box-shadow: 0 0 30px #00ccff;
            }

            100% {
                box-shadow: 0 0 60px #00ccff, 0 0 90px #00ccff;
            }
        }

        @keyframes pulseInstruction {

            0%,
            100% {
                opacity: 0.7;
            }

            50% {
                opacity: 1;
            }
        }

        @keyframes popupSlideUp {
            from {
                transform: translateY(50px) scale(0.9);
                opacity: 0;
            }

            to {
                transform: translateY(0) scale(1);
                opacity: 1;
            }
        }

        @keyframes heartbeat {

            0%,
            100% {
                transform: scale(1);
            }

            25% {
                transform: scale(1.1);
            }

            50% {
                transform: scale(1);
            }

            75% {
                transform: scale(1.05);
            }
        }

        /* Responsive Design */
        @media (max-width: 992px) {
            .photo-album {
                height: 450px;
            }

            .memories-main-title {
                font-size: 3.5rem;
            }

            .memories-subtitle {
                font-size: 2rem;
            }

            .memory-grid {
                gap: 15px;
            }

            .memory-item {
                height: 100px;
                font-size: 2.5rem;
            }
        }

        @media (max-width: 768px) {
            /* Mobile Menu Styles */
            .hamburger {
                display: flex;
            }

            .nav-menu {
                position: fixed;
                top: 0;
                left: 0;
                width: 100%;
                height: 100vh;
                background: rgba(10, 10, 42, 0.98);
                backdrop-filter: blur(20px);
                flex-direction: column;
                justify-content: center;
                align-items: center;
                gap: 25px;
                transform: translateX(100%);
                transition: transform 0.5s ease;
                z-index: 999;
            }

            .nav-menu.active {
                transform: translateX(0);
            }

            .nav-item {
                width: 100%;
                max-width: 300px;
                text-align: center;
            }

            .nav-link {
                width: 100%;
                justify-content: center;
                padding: 18px;
                font-size: 1.2rem;
                border-radius: 15px;
            }

            .memories-section {
                padding-top: 80px;
            }

            .memories-main-title {
                font-size: 3rem;
            }

            .memories-subtitle {
                font-size: 1.8rem;
            }

            .photo-album {
                height: 400px;
            }

            .album-cover,
            .album-content {
                padding: 25px;
            }

            .album-lock {
                width: 80px;
                height: 80px;
            }

            .album-lock-icon {
                font-size: 2.5rem;
            }

            .memory-grid {
                grid-template-columns: repeat(2, 1fr);
            }

            .memories-permission-content {
                padding: 30px 20px;
            }

            .memories-permission-content h2 {
                font-size: 2.5rem;
            }

            .memories-permission-buttons {
                flex-direction: column;
                align-items: center;
            }

            .memories-permission-btn {
                width: 100%;
                min-width: auto;
            }
        }

        @media (max-width: 480px) {
            .nav-logo {
                font-size: 1.5rem;
            }

            .memories-main-title {
                font-size: 2.5rem;
            }

            .memories-subtitle {
                font-size: 1.5rem;
            }

            .photo-album {
                height: 350px;
            }

            .album-lock {
                width: 70px;
                height: 70px;
            }

            .album-lock-icon {
                font-size: 2rem;
            }

            .memory-grid {
                grid-template-columns: repeat(2, 1fr);
                gap: 10px;
            }

            .memory-item {
                height: 80px;
                font-size: 2rem;
            }

            .memories-permission-content h2 {
                font-size: 2rem;
            }

            .memories-permission-content p {
                font-size: 1rem;
            }

            .memory-modal-content {
                padding: 30px 20px;
            }

            .memory-modal-content h3 {
                font-size: 2rem;
            }
        }
    </style>
</head>
<body>
    <!-- Stars Background -->
    <div class="stars" id="stars"></div>
    
    <!-- Memories Celebration Effects -->
    <div class="memories-celebration" id="memoriesCelebration"></div>
    
      <% if (gift.song) { %>
<audio id="backgroundMusic" loop>
    <source src="<%= gift.song %>" type="audio/mpeg">
</audio>
<% } %>


    <!-- Navigation Bar -->
    <%- include('Birthday_Celebration/navbar/navbar.ejs') %>
    <!-- Memories Section -->
    <section id="memories" class="route-section active memories-section">
        <!-- Memories Title -->
        <div class="memories-title-container">
            <h1 class="memories-main-title">अनमोल यादों का ख़ज़ाना</h1>
            <h2 class="memories-subtitle">वो पल जो समय के साथ और भी खास हो गए</h2>
            
            <div class="heart-divider">
                <div class="heart-divider-line"></div>
                <div class="heart-divider-icon">❤</div>
                <div class="heart-divider-line"></div>
            </div>
            
            <p style="font-size: 1.3rem; max-width: 800px; margin: 0 auto; color: #ccccff;">
                ज़िंदगी के सफ़र में कुछ पल ऐसे होते हैं,
                जो हमेशा मुस्कान बनकर दिल में बस जाते हैं।
                यह एल्बम उन्हीं खूबसूरत यादों को समर्पित है।
            </p>
        </div>
        
        <!-- Photo Album Container -->
        <div class="album-container">
            <div class="photo-album" id="photoAlbum">
                
                <!-- Album Cover -->
                <div class="album-cover">
                    <div class="album-lock">
                        <i class="fas fa-lock album-lock-icon"></i>
                    </div>

                    <h2 style="font-family: 'Great Vibes', cursive; font-size: 3.5rem; color: white; margin-bottom: 20px;">
                        यादों की एल्बम
                    </h2>

                    <p style="color: white; font-size: 1.4rem; text-align: center;">
                        हर तस्वीर, हर एहसास… एक कहानी कहता है
                    </p>

                    <p class="album-instruction">
                        एल्बम खोलने के लिए क्लिक करें
                    </p>
                </div>
                
                <!-- Album Content -->
                <div class="album-content">
                    <div class="memory-grid">
                        <div class="memory-item" data-memory="1"><i class="fas fa-heart"></i></div>
                        <div class="memory-item" data-memory="2"><i class="fas fa-star"></i></div>
                        <div class="memory-item" data-memory="3"><i class="fas fa-smile"></i></div>
                        <div class="memory-item" data-memory="4"><i class="fas fa-camera"></i></div>
                        <div class="memory-item" data-memory="5"><i class="fas fa-kiss-wink-heart"></i></div>
                        <div class="memory-item" data-memory="6"><i class="fas fa-music"></i></div>
                    </div>
                </div>
            </div>
        </div>
        
        <!-- Memory Details Modal -->
        <div class="memory-modal" id="memoryModal">
            <div class="memory-modal-content">
                <button class="close-memory" id="closeMemory">
                    <i class="fas fa-times"></i>
                </button>

                <h3 id="memoryTitle">एक प्यारी याद</h3>
                <p id="memoryDescription">
                    कुछ पल शब्दों से ज़्यादा एहसासों में बसते हैं।
                    यही वो लम्हे हैं जो ज़िंदगी को खास बनाते हैं।
                </p>

                <div style="margin-top: 30px; font-family: 'Caveat', cursive; font-size: 1.8rem; color: #ffcc00;">
                    ❤ इन यादों को हमेशा दिल के क़रीब रखें ❤
                </div>
            </div>
        </div>
        
        <!-- Permission Modal -->
        <div class="memories-permission-modal" id="memoriesPermissionModal">
            <div class="memories-permission-content">
                <h2>यादों की दुनिया में प्रवेश</h2>

                <p>
                    यह एल्बम उन पलों से भरा है,
                    जो मुस्कान, हँसी और अपनापन बिखेरते हैं।
                </p>

                <p style="font-size: 2.2rem; color: #ffcc00; margin: 30px 0;">
                    क्या आप इन खूबसूरत यादों को फिर से महसूस करना चाहेंगे?
                </p>

                <p>
                    हर याद एक एहसास है,
                    जिसे दिल से संभालकर रखा गया है।
                </p>
                
                <div class="memories-permission-footer">
                    <div class="memories-permission-buttons">
                        <button class="memories-permission-btn memories-yes-btn" id="memoriesYesBtn">
                            <i class="fas fa-heart"></i>
                            हाँ, यादों का सफ़र शुरू करें
                        </button>

                        <button class="memories-permission-btn memories-no-btn" id="memoriesNoBtn">
                            <i class="fas fa-lock"></i>
                            बाद में देखेंगे
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </section>

    <script>
        // DOM Elements
        const hamburger = document.getElementById('hamburger');
        const navMenu = document.getElementById('navMenu');
        const starsContainer = document.getElementById('stars');
        const photoAlbum = document.getElementById('photoAlbum');
        const memoriesPermissionModal = document.getElementById('memoriesPermissionModal');
        const memoriesYesBtn = document.getElementById('memoriesYesBtn');
        const memoriesNoBtn = document.getElementById('memoriesNoBtn');
        const memoriesCelebration = document.getElementById('memoriesCelebration');
        const memoryModal = document.getElementById('memoryModal');
        const closeMemory = document.getElementById('closeMemory');
        const memoryTitle = document.getElementById('memoryTitle');
        const memoryDescription = document.getElementById('memoryDescription');
        const backgroundMusic = document.getElementById('backgroundMusic');

        // Memory data
        const memories = {
            1: {
                title: "शुभ शुरुआत",
                description: "वह दिन जब हमारे परिवार में यह प्यारा रिश्ता जुड़ा और खुशियों ने एक नई शुरुआत की। यह पल हमेशा दिल में बसा रहेगा।"
            },
            2: {
                title: "खुशियों भरी शाम",
                description: "वह यादगार शाम जब हँसी, अपनापन और स्नेह ने माहौल को और भी खूबसूरत बना दिया। ऐसी यादें हमेशा मुस्कान दे जाती हैं।"
            },
            3: {
                title: "प्यारी मुस्कान",
                description: "आपकी मुस्कान पूरे माहौल को रोशन कर देती है। यह याद उसी सच्ची खुशी और अपनापन को दर्शाती है।"
            },
            4: {
                title: "साथ का सफ़र",
                description: "एक साथ बिताए गए पल और छोटी-छोटी यात्राएँ, जो रिश्तों को और भी मज़बूत बनाती हैं। हर सफ़र यादगार बन जाता है।"
            },
            5: {
                title: "अनमोल पल",
                description: "वे शांत और सुकून भरे पल जब सिर्फ़ साथ होना ही काफी होता है। यही पल रिश्तों की असली ताकत होते हैं।"
            },
            6: {
                title: "हँसी और खुशियाँ",
                description: "वह पल जब बिना किसी चिंता के सिर्फ़ हँसी और खुशी थी। ऐसे लम्हे ज़िंदगी को और भी खूबसूरत बना देते हैं।"
            }
        };

        // State Variables
        let isAlbumOpened = false;
        let hasMemoriesPermission = false;
        let memoriesSongPlayed = false;
        let isMenuOpen = false;

        // Create stars background
        function createStars() {
            const starCount = 300;
            
            for (let i = 0; i < starCount; i++) {
                const star = document.createElement('div');
                star.classList.add('star');
                
                const size = Math.random() * 3;
                const left = Math.random() * 100;
                const top = Math.random() * 100;
                const opacity = 0.2 + Math.random() * 0.8;
                const twinkleDelay = Math.random() * 5;
                
                star.style.width = `${size}px`;
                star.style.height = `${size}px`;
                star.style.left = `${left}%`;
                star.style.top = `${top}%`;
                star.style.opacity = opacity;
                star.style.animationDelay = `${twinkleDelay}s`;
                
                starsContainer.appendChild(star);
            }
        }

        // Toggle mobile menu
        function toggleMenu() {
            isMenuOpen = !isMenuOpen;

            if (isMenuOpen) {
                navMenu.classList.add('active');
                hamburger.innerHTML = '<i class="fas fa-times"></i>';
                hamburger.setAttribute('aria-expanded', 'true');
                hamburger.style.background = 'rgba(255, 102, 204, 0.3)';
                document.body.style.overflow = 'hidden';
            } else {
                navMenu.classList.remove('active');
                hamburger.innerHTML = '<i class="fas fa-bars"></i>';
                hamburger.setAttribute('aria-expanded', 'false');
                hamburger.style.background = 'rgba(255, 102, 204, 0.1)';
                document.body.style.overflow = 'auto';
            }
        }

        // Show memories permission modal
        function showMemoriesPermissionModal() {
            memoriesPermissionModal.classList.add('active');
            document.body.style.overflow = 'hidden';
        }

        // Hide memories permission modal
        function hideMemoriesPermissionModal() {
            memoriesPermissionModal.classList.remove('active');
            document.body.style.overflow = 'auto';
        }

        // Create photo frame celebration
        function createPhotoFrameCelebration() {
            const frameCount = 30;
            
            for (let i = 0; i < frameCount; i++) {
                setTimeout(() => {
                    const frame = document.createElement('div');
                    frame.classList.add('photo-frame');
                    
                    const size = 60 + Math.random() * 80;
                    const left = Math.random() * 100;
                    const duration = 3 + Math.random() * 4;
                    const delay = Math.random() * 2;
                    const emojis = ['📸', '❤️', '💖', '🌟', '✨', '📷', '💫', '🎞️'];
                    const emoji = emojis[Math.floor(Math.random() * emojis.length)];
                    
                    frame.style.left = `${left}%`;
                    frame.style.width = `${size}px`;
                    frame.style.height = `${size}px`;
                    frame.style.animation = `photoFrameFloat ${duration}s ease-in ${delay}s forwards`;
                    frame.innerHTML = emoji;
                    
                    memoriesCelebration.appendChild(frame);
                    
                    // Remove frame after animation
                    setTimeout(() => {
                        if (frame.parentNode) {
                            frame.remove();
                        }
                    }, (duration + delay) * 1000);
                }, i * 100);
            }
        }

        // Open album with celebration
        function openAlbum() {
            if (!isAlbumOpened && hasMemoriesPermission) {
                // Start celebration
                memoriesCelebration.classList.add('active');
                createPhotoFrameCelebration();
                
                // Open photo album
                photoAlbum.classList.add('open');
                isAlbumOpened = true;
                
                // Hide permission modal
                hideMemoriesPermissionModal();
                
                // Play celebration sound
                playMemoryCelebrationSound();
                
                // Show message
                setTimeout(() => {
                    alert("🎞️ हमारी यादों की गलियों में आपका स्वागत है! किसी भी याद पर क्लिक करें और उस खास पल को फिर से जीएँ। 💖");
                }, 1500);
            }
        }

        // Show memory details
        function showMemoryDetails(memoryId) {
            if (memories[memoryId]) {
                memoryTitle.textContent = memories[memoryId].title;
                memoryDescription.textContent = memories[memoryId].description;
                memoryModal.classList.add('active');
                document.body.style.overflow = 'hidden';
            }
        }

        // Close memory details
        function closeMemoryDetails() {
            memoryModal.classList.remove('active');
            document.body.style.overflow = 'auto';
        }

        // Play memory celebration sound
        function playMemoryCelebrationSound() {
            try {
                const audioContext = new (window.AudioContext || window.webkitAudioContext)();
                
                // Play nostalgic sound
                for (let i = 0; i < 3; i++) {
                    setTimeout(() => {
                        const oscillator = audioContext.createOscillator();
                        const gainNode = audioContext.createGain();
                        
                        oscillator.connect(gainNode);
                        gainNode.connect(audioContext.destination);
                        
                        oscillator.frequency.setValueAtTime(523.25 + (i * 100), audioContext.currentTime);
                        oscillator.type = 'sine';
                        
                        gainNode.gain.setValueAtTime(0.2, audioContext.currentTime);
                        gainNode.gain.exponentialRampToValueAtTime(0.01, audioContext.currentTime + 0.8);
                        
                        oscillator.start(audioContext.currentTime);
                        oscillator.stop(audioContext.currentTime + 0.8);
                    }, i * 200);
                }
            } catch (e) {
                console.log("Audio not supported");
            }
        }
if (hamburger) {
    hamburger.addEventListener('click', toggleMenu);
}

        // Play background song once
       function playBackgroundSong() {
    if (!memoriesSongPlayed && backgroundMusic) {
        backgroundMusic.currentTime = 0;
        backgroundMusic.loop = true;
        backgroundMusic.volume = 0.7;

        backgroundMusic.play().then(() => {
            memoriesSongPlayed = true;
        }).catch(() => {
            console.log("User interaction required to play audio");
        });
    }
}

        // Initialize
        window.addEventListener('load', () => {
            createStars();
            
            // Event Listeners
            hamburger.addEventListener('click', toggleMenu);
            
            photoAlbum.addEventListener('click', () => {
                if (!isAlbumOpened) {
                    showMemoriesPermissionModal();
                }
            });

            // Memory item click handlers
            document.querySelectorAll('.memory-item').forEach(item => {
                item.addEventListener('click', (e) => {
                    e.stopPropagation();
                    if (isAlbumOpened) {
                        const memoryId = item.getAttribute('data-memory');
                        showMemoryDetails(memoryId);
                    }
                });
            });

            // Yes button - Play song and open album
     // Yes button - Play song and open album
memoriesYesBtn.addEventListener('click', (e) => {
    e.stopPropagation();
    hasMemoriesPermission = true;
    playBackgroundSong();
    openAlbum();
});



            // No button - Keep album locked
            memoriesNoBtn.addEventListener('click', (e) => {
                e.stopPropagation();
                hideMemoriesPermissionModal();
                
                // Make the album shake to indicate it's staying locked
                photoAlbum.style.animation = 'none';
                setTimeout(() => {
                    photoAlbum.style.animation = 'albumFloat 8s infinite ease-in-out';
                }, 10);
                
                // Show a sweet message
                setTimeout(() => {
                    alert("मैं हमारी यादों को तब तक संभालकर रखूँगा, जब तक आप उन्हें फिर से देखने के लिए तैयार न हों। ये यादें हमेशा आपका इंतज़ार करती रहेंगी। 💝");
                }, 500);
            });

            // Close memory modal
            closeMemory.addEventListener('click', (e) => {
                e.stopPropagation();
                closeMemoryDetails();
            });

            // Close modal when clicking outside
            memoriesPermissionModal.addEventListener('click', (e) => {
                if (e.target === memoriesPermissionModal) {
                    hideMemoriesPermissionModal();
                }
            });

            memoryModal.addEventListener('click', (e) => {
                if (e.target === memoryModal) {
                    closeMemoryDetails();
                }
            });

            // Close modals with Escape key
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape') {
                    if (memoryModal.classList.contains('active')) {
                        closeMemoryDetails();
                    }
                    if (memoriesPermissionModal.classList.contains('active')) {
                        hideMemoriesPermissionModal();
                    }
                }
            });

            // Close mobile menu when clicking a link
            document.querySelectorAll('.nav-link').forEach(link => {
                link.addEventListener('click', () => {
                    if (isMenuOpen) {
                        toggleMenu();
                    }
                });
            });

            // Close menu with Escape key
            document.addEventListener('keydown', (e) => {
                if (e.key === 'Escape' && isMenuOpen) {
                    toggleMenu();
                }
            });

            // Handle window resize
            window.addEventListener('resize', () => {
                if (window.innerWidth > 768 && isMenuOpen) {
                    toggleMenu();
                }
            });

            // Welcome message
            setTimeout(() => {
                console.log(`
                ================================================
                📸 यादों का सेक्शन 📸
                ================================================
                इस एल्बम में हमारी अनमोल यादें संजोकर रखी गई हैं।
                इन्हें खोलने के लिए आपकी अनुमति आवश्यक है।
                
                अनुमति प्रक्रिया शुरू करने के लिए फोटो एल्बम पर क्लिक करें।
                ================================================
                `);
            }, 1000);
        });

        // Add CSS animations for photo frames
        const style = document.createElement('style');
        style.textContent = `
            @keyframes photoFrameFloat {
                0% {
                    transform: translateY(100vh) rotate(0deg);
                    opacity: 0;
                }
                20% {
                    opacity: 1;
                }
                80% {
                    opacity: 1;
                }
                100% {
                    transform: translateY(-100px) rotate(360deg);
                    opacity: 0;
                }
            }
        `;
        document.head.appendChild(style);
    </script>
</body>
</html>