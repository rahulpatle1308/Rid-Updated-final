{
  "name": "rts-project",
  "version": "1.0.0",
  "description": "Research Innovation & Discovery Platform",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:debug": "nodemon --inspect server.js",
    "test:db": "node -e \"require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(() => { console.log('✅ DB Test Passed'); process.exit(0); }).catch(err => { console.error('❌ DB Test Failed:', err.message); process.exit(1); });\"",
    "check:env": "node -e \"require('dotenv').config(); console.log('PORT:', process.env.PORT); console.log('MONGODB_URI exists:', !!process.env.MONGODB_URI); console.log('SESSION_SECRET exists:', !!process.env.SESSION_SECRET);\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "chart.js": "^4.5.1",
    "cookie-parser": "^1.4.7",
    "csurf": "^1.11.0",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "express-session": "^1.17.3",
    "mongoose": "^7.5.0",
    "multer": "^2.0.2",
    "pdfkit": "^0.17.2",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
