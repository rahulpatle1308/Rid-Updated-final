QuizData.questions.push(
  {
    id: "cpp_exception_1",
    topicId: "cpp_exceptions",
    question: "What is exception handling in C++? Why is it used?",
    mathSolution: "Exception handling concept",
    codeSolution:
      "Exception handling is a mechanism to handle runtime errors gracefully, allowing the program to continue execution or terminate properly.\n\nBenefits:\n1. Separates error handling code from normal code\n2. Propagates errors up the call stack\n3. Allows clean-up of resources\n4. Provides type-safe error handling",
    hint: "Exception handling = try, throw, catch. Like a safety net for runtime errors.",
  },
  {
    id: "cpp_exception_2",
    topicId: "cpp_exceptions",
    question: "What are the keywords used in C++ exception handling?",
    mathSolution: "Exception handling keywords",
    codeSolution:
      "C++ provides three keywords for exception handling:\n\n1. try: Contains code that might throw an exception\n2. throw: Throws an exception when an error occurs\n3. catch: Catches and handles exceptions thrown from try block\n\nSyntax:\ntry {\n    // code that may throw exception\n    throw SomeException();\n}\ncatch (ExceptionType e) {\n    // handle exception\n}",
    hint: "Three keywords: try (monitor), throw (raise), catch (handle).",
  },
  {
    id: "cpp_exception_3",
    topicId: "cpp_exceptions",
    question: "Write a simple program demonstrating try, throw, and catch.",
    mathSolution: "Basic exception handling",
    codeSolution:
      '#include <iostream>\nusing namespace std;\n\nint main() {\n    try {\n        int age = 15;\n        if(age < 18) {\n            throw age;  // Throw exception\n        }\n        cout << "You are eligible to vote" << endl;\n    }\n    catch(int errorAge) {\n        cout << "Exception caught! Age " << errorAge << " is less than 18" << endl;\n    }\n    \n    cout << "Program continues..." << endl;\n    return 0;\n}',
    hint: "try block contains code that might throw. throw sends exception. catch handles it.",
  },
  {
    id: "cpp_exception_4",
    topicId: "cpp_exceptions",
    question: "What types of data can be thrown as exceptions in C++?",
    mathSolution: "Throwable types",
    codeSolution:
      '#include <iostream>\n#include <string>\nusing namespace std;\n\nint main() {\n    try {\n        // Can throw any type\n        // throw 42;                    // int\n        // throw 3.14;                  // double\n        // throw "Error message";        // const char*\n        // throw string("Error");        // string\n        // throw runtime_error("Error"); // exception object\n        \n        throw 42;\n    }\n    catch(int e) {\n        cout << "Caught int: " << e << endl;\n    }\n    catch(double e) {\n        cout << "Caught double: " << e << endl;\n    }\n    catch(const char* e) {\n        cout << "Caught string: " << e << endl;\n    }\n    catch(string e) {\n        cout << "Caught C++ string: " << e << endl;\n    }\n    \n    return 0;\n}',
    hint: "You can throw ANY type in C++: int, double, string, objects, pointers, etc.",
  },
  {
    id: "cpp_exception_5",
    topicId: "cpp_exceptions",
    question:
      "What is the difference between synchronous and asynchronous exceptions?",
    mathSolution: "Synchronous vs Asynchronous",
    codeSolution:
      'Synchronous exceptions:\n- Generated by program itself\n- Occur due to logical errors (divide by zero, out of bounds)\n- Can be predicted and handled\n\nAsynchronous exceptions:\n- Generated by external events\n- Hardware failures, OS signals\n- Outside program control\n\n// Synchronous\nif(divisor == 0) throw "Division by zero";\n\n// Asynchronous (C++ doesn\'t directly handle these)\n// Hardware interrupt, power failure',
    hint: "Synchronous = program-caused (divide by zero). Asynchronous = external events (hardware failure).",
  },
  {
    id: "cpp_exception_6",
    topicId: "cpp_exceptions",
    question: "What happens if an exception is thrown but not caught?",
    mathSolution: "Uncaught exception",
    codeSolution:
      '#include <iostream>\nusing namespace std;\n\nvoid func1() { throw 100; }\nvoid func2() { func1(); }\nvoid func3() { func2(); }\n\nint main() {\n    cout << "Starting program" << endl;\n    \n    try {\n        func3();\n    }\n    catch(double d) {  // Wrong catch - won\'t catch int\n        cout << "Caught double" << endl;\n    }\n    \n    // If exception not caught, program terminates\n    cout << "This won\'t execute if exception uncaught" << endl;\n    \n    return 0;  // Program may not reach here\n}',
    hint: "Uncaught exception causes std::terminate() to be called, program aborts. Stack may not be unwound.",
  },
  {
    id: "cpp_exception_7",
    topicId: "cpp_exceptions",
    question: "How does stack unwinding work in exception handling?",
    mathSolution: "Stack unwinding",
    codeSolution:
      '#include <iostream>\nusing namespace std;\n\nclass Test {\npublic:\n    Test(int id) : id(id) { \n        cout << "Constructor " << id << endl; \n    }\n    ~Test() { \n        cout << "Destructor " << id << endl; \n    }\nprivate:\n    int id;\n};\n\nvoid func3() {\n    Test t3(3);\n    cout << "In func3, throwing exception" << endl;\n    throw 100;  // Exception thrown\n    cout << "This won\'t execute" << endl;\n}\n\nvoid func2() {\n    Test t2(2);\n    func3();\n    cout << "This won\'t execute" << endl;\n}\n\nvoid func1() {\n    Test t1(1);\n    func2();\n    cout << "This won\'t execute" << endl;\n}\n\nint main() {\n    try {\n        Test t0(0);\n        func1();\n    }\n    catch(int e) {\n        cout << "Caught exception: " << e << endl;\n    }\n    return 0;\n}',
    hint: "Stack unwinding = destructors of local objects are called as stack unwinds looking for handler.",
  },
  {
    id: "cpp_exception_8",
    topicId: "cpp_exceptions",
    question: "What is a catch-all handler? How is it written?",
    mathSolution: "Catch-all handler",
    codeSolution:
      '#include <iostream>\n#include <string>\n#include <stdexcept>\nusing namespace std;\n\nint main() {\n    try {\n        // Code that may throw different exceptions\n        int choice;\n        cout << "Enter 1-4: ";\n        cin >> choice;\n        \n        if(choice == 1) throw 42;\n        if(choice == 2) throw 3.14;\n        if(choice == 3) throw string("Error");\n        if(choice == 4) throw runtime_error("Runtime error");\n    }\n    catch(int e) {\n        cout << "Caught int: " << e << endl;\n    }\n    catch(double e) {\n        cout << "Caught double: " << e << endl;\n    }\n    catch(...) {  // Catch-all handler\n        cout << "Caught unknown exception" << endl;\n    }\n    \n    return 0;\n}',
    hint: "catch(...) catches any exception. Should be last catch block. Use for cleanup or logging.",
  },
  {
    id: "cpp_exception_9",
    topicId: "cpp_exceptions",
    question: "What is the difference between throw and throw; (rethrow)?",
    mathSolution: "Throw vs Rethrow",
    codeSolution:
      '#include <iostream>\nusing namespace std;\n\nvoid func() {\n    try {\n        throw 42;\n    }\n    catch(int e) {\n        cout << "Caught in func: " << e << endl;\n        throw;  // Rethrow the same exception\n    }\n}\n\nint main() {\n    try {\n        func();\n    }\n    catch(int e) {\n        cout << "Caught in main: " << e << endl;\n    }\n    \n    return 0;\n}',
    hint: "throw; (without arguments) rethrows current exception. throw x; throws a new exception.",
  },
  {
    id: "cpp_exception_10",
    topicId: "cpp_exceptions",
    question: "What are standard exceptions in C++? List them.",
    mathSolution: "Standard exceptions",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\n// Standard exception hierarchy\n// exception\n//   ├── bad_alloc\n//   ├── bad_cast\n//   ├── bad_typeid\n//   ├── bad_exception\n//   ├── logic_error\n//   │    ├── domain_error\n//   │    ├── invalid_argument\n//   │    ├── length_error\n//   │    └── out_of_range\n//   └── runtime_error\n//        ├── range_error\n//        ├── overflow_error\n//        └── underflow_error\n\nint main() {\n    try {\n        // vector<int> v(1000000000000); // bad_alloc\n        // string().at(10); // out_of_range\n        throw runtime_error("Runtime error example");\n    }\n    catch(const exception& e) {\n        cout << "Exception: " << e.what() << endl;\n    }\n    return 0;\n}',
    hint: "Standard exceptions inherit from std::exception. Include <stdexcept> and <exception>.",
  },
  {
    id: "cpp_exception_11",
    topicId: "cpp_exceptions",
    question: "How do you create custom exception classes?",
    mathSolution: "Custom exceptions",
    codeSolution:
      '#include <iostream>\n#include <exception>\n#include <string>\nusing namespace std;\n\n// Custom exception class\nclass InsufficientFundsException : public exception {\nprivate:\n    string message;\n    double balance;\n    double amount;\n    \npublic:\n    InsufficientFundsException(double bal, double amt) \n        : balance(bal), amount(amt) {\n        message = "Insufficient funds: balance $" + to_string(balance) \n                + ", withdrawal $" + to_string(amount);\n    }\n    \n    const char* what() const noexcept override {\n        return message.c_str();\n    }\n    \n    double getBalance() const { return balance; }\n    double getAmount() const { return amount; }\n};\n\nclass BankAccount {\nprivate:\n    double balance;\npublic:\n    BankAccount(double bal) : balance(bal) {}\n    \n    void withdraw(double amount) {\n        if(amount > balance) {\n            throw InsufficientFundsException(balance, amount);\n        }\n        balance -= amount;\n        cout << "Withdrawn: $" << amount << endl;\n    }\n};\n\nint main() {\n    BankAccount account(100);\n    \n    try {\n        account.withdraw(150);\n    }\n    catch(const InsufficientFundsException& e) {\n        cout << "Error: " << e.what() << endl;\n        cout << "Balance: $" << e.getBalance() << endl;\n        cout << "Requested: $" << e.getAmount() << endl;\n    }\n    \n    return 0;\n}',
    hint: "Derive from std::exception, override what(), add custom members and methods.",
  },
  {
    id: "cpp_exception_12",
    topicId: "cpp_exceptions",
    question: "What is the noexcept specifier? When is it used?",
    mathSolution: "noexcept specifier",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\n// Function guarantees not to throw\nvoid safeFunction() noexcept {\n    cout << "This function won\'t throw" << endl;\n}\n\n// Conditional noexcept\nvoid conditionalFunction(bool throwException) noexcept(false) {\n    if(throwException) {\n        throw runtime_error("Error!");\n    }\n}\n\n// Function that may throw\nvoid riskyFunction() {\n    throw runtime_error("Risky operation");\n}\n\nint main() {\n    cout << "safeFunction is noexcept? " << noexcept(safeFunction()) << endl;\n    cout << "riskyFunction is noexcept? " << noexcept(riskyFunction()) << endl;\n    \n    try {\n        conditionalFunction(true);\n    }\n    catch(const exception& e) {\n        cout << "Caught: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "noexcept tells compiler function won't throw. Helps with optimization and move semantics.",
  },
  {
    id: "cpp_exception_13",
    topicId: "cpp_exceptions",
    question: "What is exception safety? What are the exception safety levels?",
    mathSolution: "Exception safety levels",
    codeSolution:
      "#include <iostream>\n#include <vector>\n#include <memory>\nusing namespace std;\n\n// 1. No-throw guarantee: always succeeds\nclass NoThrow {\npublic:\n    int getValue() const noexcept { return value; }\nprivate:\n    int value;\n};\n\n// 2. Strong guarantee: if exception, state unchanged\nclass StrongGuarantee {\nprivate:\n    vector<int> data;\n    \npublic:\n    void addValue(int val) {\n        vector<int> temp = data;  // Copy\n        temp.push_back(val);      // Operation on copy\n        data = move(temp);         // Commit if successful\n    }\n};\n\n// 3. Basic guarantee: no leaks, valid state\nclass BasicGuarantee {\nprivate:\n    int* ptr;\n    \npublic:\n    BasicGuarantee() : ptr(new int(0)) {}\n    \n    void risky() {\n        *ptr = 10;  // Basic guarantee - object in valid state\n        // If something throws, ptr still points to valid memory\n    }\n    \n    ~BasicGuarantee() { delete ptr; }\n};\n\n// 4. No guarantee: may crash or corrupt\nclass NoGuarantee {\npublic:\n    void dangerous() {\n        // Anything can happen\n    }\n};",
    hint: "4 levels: No-throw, Strong (commit or rollback), Basic (valid state), No guarantee.",
  },
  {
    id: "cpp_exception_14",
    topicId: "cpp_exceptions",
    question: "How do you handle exceptions in constructors?",
    mathSolution: "Exceptions in constructors",
    codeSolution:
      '#include <iostream>\n#include <memory>\n#include <stdexcept>\nusing namespace std;\n\nclass Resource {\npublic:\n    Resource() { cout << "Resource acquired" << endl; }\n    ~Resource() { cout << "Resource released" << endl; }\n};\n\nclass MyClass {\nprivate:\n    Resource* res1;\n    Resource* res2;\n    \npublic:\n    MyClass() {\n        cout << "Constructor started" << endl;\n        res1 = new Resource();\n        \n        // Simulating an error\n        throw runtime_error("Construction failed!");\n        \n        res2 = new Resource();  // Never executed\n        cout << "Constructor finished" << endl;\n    }\n    \n    ~MyClass() {\n        delete res1;\n        delete res2;\n        cout << "Destructor called" << endl;\n    }\n};\n\n// Better approach - use RAII\nclass BetterClass {\nprivate:\n    unique_ptr<Resource> res1;\n    unique_ptr<Resource> res2;\n    \npublic:\n    BetterClass() {\n        cout << "Better constructor started" << endl;\n        res1 = make_unique<Resource>();\n        \n        // If exception here, res1 automatically cleaned up\n        throw runtime_error("Construction failed!");\n        \n        res2 = make_unique<Resource>();\n        cout << "Better constructor finished" << endl;\n    }\n};\n\nint main() {\n    try {\n        // MyClass obj;  // Resource leak!\n        BetterClass obj;  // No leak - RAII handles cleanup\n    }\n    catch(const exception& e) {\n        cout << "Caught: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "If constructor throws, destructor won't be called. Use RAII (smart pointers) for automatic cleanup.",
  },
  {
    id: "cpp_exception_15",
    topicId: "cpp_exceptions",
    question: "Can destructors throw exceptions? Why or why not?",
    mathSolution: "Exceptions in destructors",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\nclass BadDestructor {\npublic:\n    ~BadDestructor() {\n        cout << "Bad destructor" << endl;\n        throw runtime_error("Error in destructor");  // BAD!\n    }\n};\n\nclass GoodDestructor {\npublic:\n    ~GoodDestructor() noexcept {\n        cout << "Good destructor" << endl;\n        try {\n            // Cleanup code that might throw\n            // But catch and handle internally\n        }\n        catch(...) {\n            // Log error, but don\'t throw\n        }\n    }\n};\n\nint main() {\n    try {\n        BadDestructor bad;\n        // If exception active and destructor throws -> terminate()\n    }\n    catch(...) {\n        cout << "Caught something" << endl;\n    }\n    \n    cout << "Program continues?" << endl;\n    \n    return 0;\n}',
    hint: "Destructors should NEVER throw. If they do, program may terminate. Mark destructors noexcept.",
  },
  {
    id: "cpp_exception_16",
    topicId: "cpp_exceptions",
    question: "What is function-try-block? Give example.",
    mathSolution: "Function-try-block",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\nclass MyClass {\nprivate:\n    int* data;\n    \npublic:\n    // Constructor with function-try-block\n    MyClass(int size) try : data(new int[size]) {\n        cout << "Constructor body" << endl;\n        if(size <= 0) throw invalid_argument("Invalid size");\n    }\n    catch(const exception& e) {\n        cout << "Constructor caught: " << e.what() << endl;\n        // data already cleaned up because new failed\n        throw;  // Must rethrow\n    }\n    \n    // Destructor with function-try-block (rarely needed)\n    ~MyClass() noexcept try {\n        delete[] data;\n    }\n    catch(...) {\n        // Catch but don\'t throw\n    }\n};\n\n// Regular function with function-try-block\nint divide(int a, int b) try {\n    if(b == 0) throw runtime_error("Division by zero");\n    return a / b;\n}\ncatch(const exception& e) {\n    cout << "Function caught: " << e.what() << endl;\n    throw;  // Rethrow\n}\n\nint main() {\n    try {\n        MyClass obj(-5);\n    }\n    catch(const exception& e) {\n        cout << "Main caught: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "Function-try-block catches exceptions from member initializer list and function body. Constructor must rethrow.",
  },
  {
    id: "cpp_exception_17",
    topicId: "cpp_exceptions",
    question:
      "What is the difference between std::exception and std::runtime_error?",
    mathSolution: "exception vs runtime_error",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\nint main() {\n    try {\n        // std::exception is base class\n        throw std::exception();\n    }\n    catch(const std::exception& e) {\n        cout << "exception: " << e.what() << endl;  // Implementation-defined\n    }\n    \n    try {\n        // runtime_error accepts string message\n        throw std::runtime_error("Something went wrong");\n    }\n    catch(const std::runtime_error& e) {\n        cout << "runtime_error: " << e.what() << endl;  // Custom message\n    }\n    \n    try {\n        // Can catch both with base class reference\n        throw std::runtime_error("Test");\n    }\n    catch(const std::exception& e) {\n        cout << "Base caught: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "exception is base (generic). runtime_error is derived, accepts string message in constructor.",
  },
  {
    id: "cpp_exception_18",
    topicId: "cpp_exceptions",
    question: "How do you handle multiple exceptions?",
    mathSolution: "Multiple catch blocks",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\nvoid processInput(int value) {\n    if(value < 0) {\n        throw invalid_argument("Negative value not allowed");\n    }\n    if(value == 0) {\n        throw runtime_error("Zero value special case");\n    }\n    if(value > 100) {\n        throw out_of_range("Value exceeds maximum");\n    }\n    if(value == 42) {\n        throw 42;  // Non-standard exception\n    }\n    cout << "Processing " << value << endl;\n}\n\nint main() {\n    int inputs[] = {-5, 0, 150, 42, 50};\n    \n    for(int val : inputs) {\n        try {\n            cout << "\\nTrying value " << val << ": ";\n            processInput(val);\n        }\n        catch(const invalid_argument& e) {\n            cout << "Invalid argument: " << e.what() << endl;\n        }\n        catch(const runtime_error& e) {\n            cout << "Runtime error: " << e.what() << endl;\n        }\n        catch(const out_of_range& e) {\n            cout << "Out of range: " << e.what() << endl;\n        }\n        catch(int e) {\n            cout << "Caught integer: " << e << endl;\n        }\n        catch(...) {\n            cout << "Unknown exception" << endl;\n        }\n    }\n    \n    return 0;\n}',
    hint: "Use multiple catch blocks, order from most specific to most general. catch(...) for anything else.",
  },
  {
    id: "cpp_exception_19",
    topicId: "cpp_exceptions",
    question: "What is exception specification? Why was it deprecated?",
    mathSolution: "Exception specifications",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\n// Old style (deprecated C++98)\nvoid oldFunction() throw(int, double) {  // Can only throw int or double\n    throw 42;  // OK\n    // throw string("error");  // Would call unexpected()\n}\n\n// Modern C++11 style\nvoid modernFunction() noexcept;  // Won\'t throw\nvoid modernFunction2() noexcept(false);  // May throw\n\n// noexcept operator\nvoid test() {\n    cout << "noexcept(oldFunction()): " << noexcept(oldFunction()) << endl;\n    cout << "noexcept(modernFunction()): " << noexcept(modernFunction()) << endl;\n}\n\nint main() {\n    test();\n    return 0;\n}',
    hint: "Old throw() specifications deprecated. Use noexcept (C++11) instead. Simpler and more efficient.",
  },
  {
    id: "cpp_exception_20",
    topicId: "cpp_exceptions",
    question: "How do you write exception-safe code?",
    mathSolution: "Exception-safe code",
    codeSolution:
      '#include <iostream>\n#include <memory>\n#include <vector>\n#include <stdexcept>\nusing namespace std;\n\n// 1. Use RAII (Resource Acquisition Is Initialization)\nclass DatabaseConnection {\nprivate:\n    unique_ptr<int> connection;\npublic:\n    DatabaseConnection() : connection(new int(1)) {\n        cout << "Connection opened" << endl;\n    }\n    ~DatabaseConnection() {\n        cout << "Connection closed" << endl;\n    }\n    void query(const string& q) { \n        if(q.empty()) throw runtime_error("Empty query");\n        cout << "Executing: " << q << endl;\n    }\n};\n\n// 2. Strong guarantee - commit or rollback\nclass Transaction {\nprivate:\n    DatabaseConnection& conn;\n    bool committed;\n    \npublic:\n    Transaction(DatabaseConnection& c) : conn(c), committed(false) {\n        cout << "Transaction started" << endl;\n    }\n    \n    void commit() {\n        if(!committed) {\n            cout << "Transaction committed" << endl;\n            committed = true;\n        }\n    }\n    \n    ~Transaction() {\n        if(!committed) {\n            cout << "Transaction rolled back (destructor)" << endl;\n        }\n    }\n};\n\n// 3. Copy-and-swap idiom\nclass DataHolder {\nprivate:\n    vector<int> data;\n    \npublic:\n    void add(int value) {\n        vector<int> temp = data;  // Copy\n        temp.push_back(value);     // Operation on copy\n        data.swap(temp);           // Commit (no-throw)\n    }\n};\n\n// 4. No-throw swap\nclass MyClass {\nprivate:\n    int* ptr;\npublic:\n    void swap(MyClass& other) noexcept {\n        std::swap(ptr, other.ptr);\n    }\n};\n\nint main() {\n    try {\n        DatabaseConnection db;\n        \n        {\n            Transaction txn(db);\n            db.query("SELECT * FROM users");\n            db.query("");  // This throws!\n            txn.commit();  // Never reached\n        }\n    }\n    catch(const exception& e) {\n        cout << "Caught: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "RAII, strong guarantee (commit/rollback), copy-and-swap, no-throw swap, avoid exceptions in destructors.",
  },
  {
    id: "cpp_exception_21",
    topicId: "cpp_exceptions",
    question:
      "What is the difference between std::logic_error and std::runtime_error?",
    mathSolution: "logic_error vs runtime_error",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\nint divide(int a, int b) {\n    if(b == 0) {\n        // Runtime error - can\'t be prevented at compile time\n        throw runtime_error("Division by zero");\n    }\n    return a / b;\n}\n\nint squareRoot(int x) {\n    if(x < 0) {\n        // Logic error - should be caught during development\n        throw logic_error("Cannot take square root of negative number");\n    }\n    return x; // Simplified\n}\n\nint main() {\n    try {\n        // Runtime error - input dependent\n        int x = 10, y = 0;\n        cout << divide(x, y) << endl;\n    }\n    catch(const runtime_error& e) {\n        cout << "Runtime error: " << e.what() << endl;\n    }\n    \n    try {\n        // Logic error - programming mistake\n        cout << squareRoot(-5) << endl;\n    }\n    catch(const logic_error& e) {\n        cout << "Logic error: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "logic_error = preventable programming errors (pre-runtime). runtime_error = unpredictable runtime conditions.",
  },
  {
    id: "cpp_exception_22",
    topicId: "cpp_exceptions",
    question: "How do you handle memory allocation failures?",
    mathSolution: "bad_alloc exception",
    codeSolution:
      '#include <iostream>\n#include <new>\n#include <vector>\nusing namespace std;\n\nint main() {\n    // Standard behavior - throws bad_alloc\n    try {\n        // Attempt to allocate huge memory\n        int* p = new int[1000000000000];  // May throw bad_alloc\n        delete[] p;\n    }\n    catch(const bad_alloc& e) {\n        cout << "Memory allocation failed: " << e.what() << endl;\n    }\n    \n    // Using nothrow version - returns nullptr instead of throwing\n    int* p2 = new(nothrow) int[1000000000000];\n    if(p2 == nullptr) {\n        cout << "Memory allocation failed (nothrow)" << endl;\n    } else {\n        delete[] p2;\n    }\n    \n    // Vector also throws bad_alloc\n    try {\n        vector<int> v;\n        v.reserve(1000000000000);  // May throw bad_alloc\n    }\n    catch(const bad_alloc& e) {\n        cout << "Vector allocation failed: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "new throws std::bad_alloc on failure. Use new(std::nothrow) for nullptr on failure.",
  },
  {
    id: "cpp_exception_23",
    topicId: "cpp_exceptions",
    question: "What is exception neutrality?",
    mathSolution: "Exception neutrality",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\nusing namespace std;\n\n// Exception neutral function - passes exceptions through\nvoid exceptionNeutral(int x) {\n    cout << "Neutral function started" << endl;\n    \n    // Don\'t catch exceptions - let them propagate\n    if(x < 0) throw invalid_argument("Negative value");\n    if(x == 0) throw runtime_error("Zero value");\n    \n    cout << "Neutral function ended" << endl;\n}\n\n// Exception safe wrapper - catches and translates\nvoid exceptionSafe(int x) {\n    try {\n        exceptionNeutral(x);\n    }\n    catch(const invalid_argument& e) {\n        // Translate to different exception\n        cout << "Translating exception" << endl;\n        throw runtime_error(string("Invalid: ") + e.what());\n    }\n    catch(const runtime_error& e) {\n        // Log and rethrow\n        cout << "Logging: " << e.what() << endl;\n        throw;  // Rethrow same exception\n    }\n}\n\nint main() {\n    for(int val : {-1, 0, 5}) {\n        try {\n            cout << "\\nTesting value " << val << ":" << endl;\n            exceptionSafe(val);\n        }\n        catch(const exception& e) {\n            cout << "Main caught: " << e.what() << endl;\n        }\n    }\n    \n    return 0;\n}',
    hint: "Exception neutrality = function doesn't interfere with exception flow - lets them pass through or translates them appropriately.",
  },
  {
    id: "cpp_exception_24",
    topicId: "cpp_exceptions",
    question: "How do you handle exceptions in multi-threaded programs?",
    mathSolution: "Exceptions in threads",
    codeSolution:
      '#include <iostream>\n#include <thread>\n#include <future>\n#include <stdexcept>\n#include <exception>\nusing namespace std;\n\n// Problem: Exceptions can\'t cross thread boundaries directly\nvoid threadFunction() {\n    cout << "Thread started" << endl;\n    throw runtime_error("Error in thread");\n    // Exception dies with thread\n}\n\n// Solution 1: Use async and future\nint asyncFunction() {\n    throw runtime_error("Error in async task");\n    return 42;\n}\n\n// Solution 2: Catch and store exception\nvoid safeThreadFunction(exception_ptr& eptr) {\n    try {\n        cout << "Safe thread started" << endl;\n        throw runtime_error("Error in safe thread");\n    }\n    catch(...) {\n        eptr = current_exception();  // Store exception\n    }\n}\n\n// Solution 3: Use packaged_task\nint riskyFunction(int x) {\n    if(x < 0) throw invalid_argument("Negative not allowed");\n    return x * x;\n}\n\nint main() {\n    cout << "=== Thread exceptions ===" << endl;\n    \n    // Problem: Exception lost\n    thread t1(threadFunction);\n    t1.join();  // Exception already lost\n    \n    // Solution 1: future from async\n    cout << "\\n=== Using async/future ===" << endl;\n    try {\n        auto fut = async(launch::async, asyncFunction);\n        int result = fut.get();  // Exception propagated here\n    }\n    catch(const exception& e) {\n        cout << "Caught from async: " << e.what() << endl;\n    }\n    \n    // Solution 2: Store exception_ptr\n    cout << "\\n=== Using exception_ptr ===" << endl;\n    exception_ptr eptr;\n    thread t2(safeThreadFunction, ref(eptr));\n    t2.join();\n    \n    if(eptr) {\n        try {\n            rethrow_exception(eptr);\n        }\n        catch(const exception& e) {\n            cout << "Caught stored exception: " << e.what() << endl;\n        }\n    }\n    \n    // Solution 3: packaged_task\n    cout << "\\n=== Using packaged_task ===" << endl;\n    packaged_task<int(int)> task(riskyFunction);\n    auto fut = task.get_future();\n    \n    thread t3(move(task), -5);\n    t3.join();\n    \n    try {\n        int result = fut.get();\n    }\n    catch(const exception& e) {\n        cout << "Caught from packaged_task: " << e.what() << endl;\n    }\n    \n    return 0;\n}',
    hint: "Thread exceptions can't cross boundaries. Use async/future, exception_ptr, or packaged_task to propagate.",
  },
  {
    id: "cpp_exception_25",
    topicId: "cpp_exceptions",
    question:
      "Write a complete program demonstrating comprehensive exception handling.",
    mathSolution: "Complete exception handling demo",
    codeSolution:
      '#include <iostream>\n#include <stdexcept>\n#include <memory>\n#include <vector>\n#include <fstream>\n#include <exception>\nusing namespace std;\n\n// Custom exception class\nclass FileOperationException : public exception {\nprivate:\n    string message;\n    string filename;\n    int errorCode;\n    \npublic:\n    FileOperationException(const string& file, int code, const string& msg)\n        : filename(file), errorCode(code), message(msg) {}\n    \n    const char* what() const noexcept override {\n        return message.c_str();\n    }\n    \n    string getFilename() const { return filename; }\n    int getErrorCode() const { return errorCode; }\n};\n\n// Resource class with RAII\nclass FileHandler {\nprivate:\n    FILE* file;\n    string filename;\n    \npublic:\n    FileHandler(const string& fname, const string& mode) : filename(fname) {\n        cout << "Opening file: " << filename << endl;\n        file = fopen(fname.c_str(), mode.c_str());\n        \n        if(!file) {\n            throw FileOperationException(filename, errno, \n                "Failed to open file: " + filename);\n        }\n    }\n    \n    void write(const string& data) {\n        if(fwrite(data.c_str(), 1, data.size(), file) != data.size()) {\n            throw FileOperationException(filename, errno,\n                "Failed to write to file: " + filename);\n        }\n    }\n    \n    string read() {\n        char buffer[256];\n        if(fgets(buffer, sizeof(buffer), file) == nullptr) {\n            if(feof(file)) {\n                return "";  // End of file\n            }\n            throw FileOperationException(filename, errno,\n                "Failed to read from file: " + filename);\n        }\n        return string(buffer);\n    }\n    \n    ~FileHandler() noexcept {\n        if(file) {\n            cout << "Closing file: " << filename << endl;\n            fclose(file);\n        }\n    }\n};\n\n// Function demonstrating strong exception guarantee\nvector<int> processNumbers(const vector<int>& input) {\n    vector<int> result;\n    result.reserve(input.size());\n    \n    try {\n        for(int num : input) {\n            if(num < 0) {\n                throw invalid_argument("Negative numbers not allowed: " + to_string(num));\n            }\n            if(num > 1000) {\n                throw overflow_error("Number too large: " + to_string(num));\n            }\n            result.push_back(num * num);\n        }\n    }\n    catch(...) {\n        // Strong guarantee - if exception, result is unchanged\n        // Since result is local, it will be destroyed automatically\n        throw;  // Rethrow\n    }\n    \n    return result;\n}\n\n// Function with multiple exception types\ndouble safeDivide(double a, double b) {\n    if(b == 0) {\n        throw runtime_error("Division by zero");\n    }\n    if(isnan(a) || isnan(b)) {\n        throw domain_error("NaN values not allowed");\n    }\n    return a / b;\n}\n\n// Noexcept function\nvoid logError(const string& error) noexcept {\n    try {\n        ofstream log("error.log", ios::app);\n        if(log) {\n            log << "Error: " << error << endl;\n        }\n    }\n    catch(...) {\n        // Swallow any exceptions - can\'t throw from noexcept\n    }\n}\n\nint main() {\n    cout << "=== Comprehensive Exception Handling Demo ===\\n" << endl;\n    \n    // 1. RAII and custom exceptions\n    cout << "1. File handling with RAII:" << endl;\n    try {\n        FileHandler file("test.txt", "w");\n        file.write("Hello, World!\\n");\n        // File automatically closed when handler goes out of scope\n    }\n    catch(const FileOperationException& e) {\n        cout << "File error: " << e.what() << endl;\n        cout << "File: " << e.getFilename() << ", Code: " << e.getErrorCode() << endl;\n        logError(e.what());\n    }\n    \n    // 2. Strong guarantee example\n    cout << "\\n2. Strong guarantee example:" << endl;\n    vector<int> data = {1, 2, 3, -4, 5};\n    \n    try {\n        vector<int> processed = processNumbers(data);\n        cout << "Processed numbers: ";\n        for(int n : processed) cout << n << " ";\n        cout << endl;\n    }\n    catch(const exception& e) {\n        cout << "Processing failed: " << e.what() << endl;\n        // Original data unchanged\n        cout << "Original data intact: ";\n        for(int n : data) cout << n << " ";\n        cout << endl;\n    }\n    \n    // 3. Multiple catch blocks\n    cout << "\\n3. Multiple exception types:" << endl;\n    try {\n        cout << "Result: " << safeDivide(10, 0) << endl;\n    }\n    catch(const runtime_error& e) {\n        cout << "Runtime error: " << e.what() << endl;\n    }\n    catch(const domain_error& e) {\n        cout << "Domain error: " << e.what() << endl;\n    }\n    catch(const exception& e) {\n        cout << "Other exception: " << e.what() << endl;\n    }\n    \n    // 4. Function-try-block\n    cout << "\\n4. Function-try-block:" << endl;\n    try {\n        // Using a lambda with function-try-block\n        auto riskyLambda = [](int x) try {\n            if(x == 0) throw runtime_error("Zero input");\n            return 100 / x;\n        }\n        catch(const exception& e) {\n            cout << "Lambda caught: " << e.what() << endl;\n            throw;  // Rethrow\n        };\n        \n        cout << "Result: " << riskyLambda(0) << endl;\n    }\n    catch(const exception& e) {\n        cout << "Main caught rethrown: " << e.what() << endl;\n    }\n    \n    // 5. Exception neutrality\n    cout << "\\n5. Exception neutrality:" << endl;\n    auto neutralFunc = [](bool shouldThrow) {\n        cout << "Neutral function executing" << endl;\n        if(shouldThrow) throw logic_error("Something went wrong");\n        cout << "Neutral function completed" << endl;\n    };\n    \n    auto wrapperFunc = [&](bool shouldThrow) {\n        try {\n            neutralFunc(shouldThrow);\n        }\n        catch(const logic_error& e) {\n            cout << "Wrapper caught and translated: " << e.what() << endl;\n            throw runtime_error("Translated error");\n        }\n    };\n    \n    try {\n        wrapperFunc(true);\n    }\n    catch(const exception& e) {\n        cout << "Final handler: " << e.what() << endl;\n    }\n    \n    // 6. noexcept guarantee\n    cout << "\\n6. noexcept guarantee:" << endl;\n    logError("Test error message");\n    cout << "Noexcept function called successfully" << endl;\n    \n    // 7. catch-all handler\n    cout << "\\n7. catch-all handler:" << endl;\n    try {\n        int choice;\n        cout << "Enter 1 for int, 2 for double, 3 for string: ";\n        cin >> choice;\n        \n        if(choice == 1) throw 42;\n        if(choice == 2) throw 3.14;\n        if(choice == 3) throw string("Unknown type");\n    }\n    catch(int e) {\n        cout << "Caught int: " << e << endl;\n    }\n    catch(double e) {\n        cout << "Caught double: " << e << endl;\n    }\n    catch(...) {\n        cout << "Caught unknown exception type" << endl;\n    }\n    \n    cout << "\\n=== End of Exception Handling Demo ===" << endl;\n    \n    return 0;\n}',
    hint: "This demonstrates: custom exceptions, RAII, strong guarantee, multiple catches, function-try-blocks, exception neutrality, noexcept, and catch-all.",
  },
); 
